import { PopAnimateDirective } from './pop-animate.directive';

describe('PopAnimateDirective', () => {
  it('should create an instance', () => {
    const directive = new PopAnimateDirective();
    expect(directive).toBeTruthy();
  });
});
